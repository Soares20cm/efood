{
  "buildCommand": "cd efood && npm install && npm run build",
  "outputDirectory": "efood/build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
